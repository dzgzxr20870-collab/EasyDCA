VITE_API_BASE_URL=https://easydca-production.up.railway.app

# ── Feature Flag: Dashboard แบบแยกหน้า (/app/*) — Stage 9 ─────────────────
# ค่าที่รับได้: 'true' เท่านั้นที่เปิด · ค่าอื่น/ไม่ตั้งเลย = ปิด (Fail-closed)
# ปิดอยู่ = Route /app/* ไม่มีในแอปเลย · /dashboard เดิมไม่ถูกแทนที่ไม่ว่ากรณีใด
# ⚠️ Vite ฝังค่าตอน Build — เปลี่ยนแล้วต้อง Redeploy เสมอ
# VITE_ENABLE_MULTIPAGE_APP=true
